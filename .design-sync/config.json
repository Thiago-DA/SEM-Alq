{
  "projectId": "0f707fbd-bf3d-4fbc-a8f3-74c68d6e4cb7",
  "shape": "package",
  "pkg": "@rentar/ui",
  "globalName": "RentarUI",
  "buildCmd": "npm run build:types --workspace=@rentar/ui",
  "cssEntry": "src/design-sync-fonts.css",
  "provider": {
    "component": "ThemeProvider",
    "props": {}
  },
  "readmeHeader": ".design-sync/conventions.md",
  "dtsPropsFor": {
    "StatusTag": "  /** Dominio del estado (`'propiedad'`, `'contrato'`, etc.). En el componente real, `domain` fija qué subconjunto de `status` es válido (genérico sobre `D extends StatusDomain`) — el extractor de `.d.ts` no preserva esa correlación entre dos tipos de otro paquete, así que acá va la unión completa de cada campo por separado. */\n  domain: \"propiedad\" | \"contrato\" | \"firma\" | \"cobro\" | \"reclamo\" | \"suscripcion\" | \"solicitud\" | \"usuario\" | \"factura\";\n  /** Estado dentro de ese dominio. Unión de TODOS los estados de TODOS los dominios (ver la nota en `domain`) — en uso real, el valor válido depende del `domain` elegido. */\n  status: \"publicada\" | \"pausada\" | \"alquilada\" | \"alquilada_publicada\" | \"pendiente_firma\" | \"vigente\" | \"finalizado\" | \"rescindido\" | \"pendiente\" | \"firmado\" | \"rechazado\" | \"pagado\" | \"vencido\" | \"anulado\" | \"parcial\" | \"abierto\" | \"en_proceso\" | \"resuelto\" | \"cerrado\" | \"activa\" | \"vencida\" | \"cancelada\" | \"aceptada\" | \"rechazada\" | \"activo\" | \"suspendido\" | \"sin_verificar\" | \"pagada\";\n  \"data-testid\"?: string;",
    "DataTable": "  /** Cada columna: `render` recibe la fila (`unknown` acá — el componente real es genérico sobre `T`, no preservable en este `.d.ts` plano). */\n  columns: { key: string; title: string; render: (record: unknown) => React.ReactNode }[];\n  data: unknown[];\n  rowKey: (record: unknown) => string;\n  loading?: boolean;\n  emptyDescription?: string;\n  \"data-testid\"?: string;"
  },
  "overrides": {
    "DataTable": {
      "cardMode": "column"
    },
    "Header": {
      "cardMode": "single",
      "viewport": "1200x120"
    },
    "PublicLayout": {
      "cardMode": "single",
      "viewport": "1200x560"
    },
    "RoleSwitcher": {
      "cardMode": "single",
      "primaryStory": "Default"
    },
    "ConfirmActionModal": {
      "cardMode": "single",
      "primaryStory": "Default"
    },
    "AppShell": {
      "cardMode": "single",
      "viewport": "1200x640"
    }
  }
}
